# Site data (bookings, edited content, the admin password) lives outside
# public_html entirely now — see api/_lib.php and DEPLOY.md — specifically
# so it can never be committed here in the first place. Nothing to ignore
# for that reason anymore; this file is kept for anything added later.
